# Per-adapter caller so SQLite gets its own status badge (see the README
# "Database compatibility" matrix). Logic lives in the reusable integration.yml.
name: SQLite

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  integration:
    uses: ./.github/workflows/integration.yml
    with:
      adapter: sqlite
