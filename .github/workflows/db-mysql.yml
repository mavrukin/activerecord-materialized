# Per-adapter caller so MySQL gets its own status badge (see the README
# "Database compatibility" matrix). Logic lives in the reusable integration.yml.
name: MySQL

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  integration:
    uses: ./.github/workflows/integration.yml
    with:
      adapter: mysql
